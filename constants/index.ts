/**
 * Constants Index
 * Central export point for all application constants
 */

export * from "./theme";

// Re-export commonly used constants for convenience
export {
  spacing,
  responsiveSpacing,
  typography,
  buttonSizes,
  gaps,
  radius,
  shadows,
  containers,
  zIndex,
  iconSizes,
  theme,
} from "./theme";
